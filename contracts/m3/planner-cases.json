{
  "schema_version": "boring-cdc/m3-planner-cases/v1",
  "owner_bead": "boring-cdc-m3-planner",
  "cases": [
    {"scenario_id":"SCN-M3-PLANNER-KEY-FORMS","unit_test":"canonical_signed_uuid_and_composite_order_property","expected":"signed integer, UUID, and composite keys retain PostgreSQL ordering"},
    {"scenario_id":"SCN-M3-PLANNER-RANGES","unit_test":"half_open_ranges_cover_sparse_min_max_without_overlap","expected":"empty, sparse, minimum, and maximum fixtures are covered exactly once"},
    {"scenario_id":"SCN-M3-PLANNER-REJECT-KEY","unit_test":"null_partial_and_wrong_type_fail_closed","expected":"null, partial, and mismatched keys are rejected"},
    {"scenario_id":"SCN-M3-PLANNER-KEYSET","unit_test":"query_is_keyset_only_and_composite","expected":"canonical half-open tuple predicates contain no OFFSET or ctid"},
    {"scenario_id":"SCN-M3-PLANNER-POSTGRES","unit_test":"live_postgres_worker_executes_persisted_composite_ranges","expected":"Rust worker consumes a bootstrap-owned exported-snapshot importer capability against PostgreSQL 17.6 twice and refuses over-budget metadata before payload fetch"},
    {"scenario_id":"SCN-M3-PLANNER-PEAK-MEMORY","unit_test":"postgres_copy_peak_accounts_for_all_simultaneous_allocations","expected":"peak accounting includes vector capacities, canonical and cloned key buffers, payload buffers, and simultaneous metadata/hex decode data"},
    {"scenario_id":"SCN-M3-PLANNER-BOOTSTRAP-BINDING","unit_test":"persisted_chunks_resume_and_event_commit_is_atomic","expected":"plan admission requires exact promotable bootstrap generation, importer assignment, schema, epoch, and start sequence"},
    {"scenario_id":"SCN-M3-PLANNER-RESUME","unit_test":"persisted_chunks_resume_and_event_commit_is_atomic","expected":"pending ranges survive reopen and injected commit failure rolls back events"},
    {"scenario_id":"SCN-M3-PLANNER-LIMITS","unit_test":"row_byte_time_rate_source_and_range_bounds_are_enforced","expected":"row, byte, time, source-impact, rate, and range limits fail closed"},
    {"scenario_id":"SCN-M3-PLANNER-STALE","unit_test":"limits_concurrency_and_stale_generation_are_enforced","expected":"bounded concurrency and generation fencing reject stale completion"},
    {"scenario_id":"SCN-M3-PLANNER-EXECUTION-FENCE","unit_test":"execution_reloads_range_bootstrap_and_expiry","expected":"execution reloads persisted range and bootstrap eligibility and rejects expiry during source read"},
    {"scenario_id":"SCN-M3-PLANNER-ETA","unit_test":"progress_eta_is_redacted_and_deterministic","expected":"persisted progress and ETA expose no canonical key material"},
    {"scenario_id":"SCN-M3-PLANNER-READER-AGE","unit_test":"persisted_chunks_resume_and_event_commit_is_atomic","expected":"bounded M2 reader is dropped before caller awaits external work"},
    {"scenario_id":"SCN-M3-PLANNER-ATOMIC-COMMIT","unit_test":"persisted_chunks_resume_and_event_commit_is_atomic","expected":"snapshot events and completion marker share one SQLite transaction"}
  ]
}
