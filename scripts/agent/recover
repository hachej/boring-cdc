#!/usr/bin/env python3
"""Explain recovery inputs. Recovery remains an explicit human-run operation."""
import json,sys
bead=sys.argv[1] if len(sys.argv)>1 else None
print(json.dumps({"schema_version":"agent-recover/v1","bead_id":bead,"read_only":True,"performed":False,"underlying_commands":["git status --short --branch",f"br show {bead}" if bead else "br ready"],"reason":"automatic claims and product mutations are forbidden"},sort_keys=True))
