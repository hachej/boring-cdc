#!/usr/bin/env python3
"""Explain the verification tier and underlying commands; never execute them."""
import json,sys
bead=sys.argv[1] if len(sys.argv)>1 else "boring-cdc-m0-scaffold"
print(json.dumps({"schema_version":"agent-verify/v1","bead_id":bead,"tier":"component","read_only":True,"commands":["cargo fmt --all -- --check","cargo clippy --locked --workspace --all-targets -- -D warnings","cargo test --locked --workspace --all-targets","scripts/e2e/m0_scaffold.sh","scripts/faults/m0_scaffold.sh"],"reason":"scaffold acceptance declares evidence_tier=component"},sort_keys=True))
